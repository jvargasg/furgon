{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Apoderado list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombres</th>
                <th>Appaterno</th>
                <th>Apmaterno</th>
                <th>Rut</th>
                <th>Dv</th>
                <th>Direccion</th>
                <th>Telefono</th>
                <th>Celular</th>
                <th>Mail</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('apoderado_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombres }}</td>
                <td>{{ entity.apPaterno }}</td>
                <td>{{ entity.apMaterno }}</td>
                <td>{{ entity.rut }}</td>
                <td>{{ entity.dv }}</td>
                <td>{{ entity.direccion }}</td>
                <td>{{ entity.telefono }}</td>
                <td>{{ entity.celular }}</td>
                <td>{{ entity.mail }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('apoderado_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('apoderado_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('apoderado_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
